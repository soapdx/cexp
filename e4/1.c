#include<stdio.h>
int main()
{
 int a;
	scanf("%d",&a);
	printf("a=%d\n",a);

	if(a>0)
		printf("a=%d 是正数\n",a);
	else
		printf("a=%d 不是正数\n",a);

	if(a>0)
		printf("a=%d 是正数\n",a);
	    printf("a=%d 不是正数\n",a);

	if(a>0)
	{
		printf("a=%d 是正数\n",a);
		printf("a=%d 不是正数\n",a);
	}
 return 0;
}
